{
  "devDependencies": {
    "cross-env": "^10.1.0",
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}
"scripts": {
  "test": "jest --runInBand",
  "test:unit": "jest --runInBand --testPathPattern=unit",
  "test:integration": "jest --runInBand --testPathPattern=integration"
}
"scripts": {
  "install-all": "npm --prefix client install && npm --prefix server install",
  "setup-test-db": "cd server && npm run setup-test-db",
  "test": "npm run test:unit && npm run test:integration && npm run test:e2e",
  "test:unit": "npm run test:unit --prefix server && npm run test:unit --prefix client",
  "test:integration": "npm run test:integration --prefix server && npm run test:integration --prefix client",
  "test:e2e": "npx cypress run"
}
